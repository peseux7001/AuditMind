-- AuditMind accountant review runtime seed.
-- Review rows are created only from customer submission processing results.
-- Do not seed demo companies, submission items, uploaded files, or classification results.

SELECT 1;
